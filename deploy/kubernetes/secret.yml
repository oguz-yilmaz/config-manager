apiVersion: v1
kind: Secret
metadata:
    name: config-service-secrets
    namespace: config-system
type: Opaque
data:
    # These are base64 encoded values
    auth-secret-key: YOUR_BASE64_ENCODED_SECRET
    etcd-password: YOUR_BASE64_ENCODED_PASSWORD
